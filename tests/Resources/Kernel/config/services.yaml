parameters:
    test_parameter: 'This is a test'

services:
    _defaults:
        autoconfigure: true
        autowire: true
        public: true

    RichId\AutoconfigureBundle\Tests\Resources\:
        resource: '../../'
        exclude:
            - '../../Kernel'
            - '../../Event'
